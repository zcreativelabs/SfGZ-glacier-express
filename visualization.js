
document.addEventListener("DOMContentLoaded", () => {
  d3.json("/country.json")
    .then(country => {
      d3.json("/route-stops.json")
        .then(routeStops => {

          // ==================================================

          // Visualisierung...
          
          // ==================================================

        })
    })
})
